{
  "name": "enginerds-lead-engine",
  "version": "2.1.0",
  "description": "AI Lead Engine CRM with Gmail OAuth and tracking - Pure Neon Postgres",
  "scripts": {
    "dev": "node server.js",
    "deploy": "vercel --prod",
    "build": "cd crm-ui && npm install && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "nodemailer": "^6.9.13"
  },
  "engines": {
    "node": "24.x"
  }
}
