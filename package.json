{
  "name": "enginerds-lead-engine",
  "version": "2.0.0",
  "description": "AI Lead Engine CRM with Gmail OAuth and tracking",
  "scripts": {
    "dev": "node server.js",
    "deploy": "vercel --prod",
    "build": "cd crm-ui && npm install && npm run build"
  },
  "dependencies": {
    "nodemailer": "^6.9.13"
  },
  "engines": {
    "node": "20.x"
  }
}
